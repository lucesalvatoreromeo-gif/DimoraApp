import { Language } from "../types";

// Questa funzione ora non fa nulla, restituisce solo il testo originale.
// L'IA è stata rimossa per semplificare il deploy.
export const polishText = async (text: string, language: Language): Promise<string> => {
  return text;
};